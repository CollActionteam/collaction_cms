import 'package:flutter/material.dart';

class BlocksRadioTable extends StatelessWidget {
  const BlocksRadioTable({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      width: 350,
      height: 230,
    );
  }
}
