import 'package:flutter/material.dart';

class CrowdActionPage extends StatelessWidget {
  const CrowdActionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('CrowdActions');
  }
}
